The scripts in this folder are used to analyze data created 
from the Snakemake rules.

Initially it contains scripts to run GERP from GERP++ using
the output of the roast_gerp.smk rule.

It contains both scripts that perform specific functionality,
and the scripts needed by docker to invoke these specific
funcitonality scripts.

